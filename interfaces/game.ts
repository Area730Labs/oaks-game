export interface GameType {
    created_at: number,
    hash: string
    uid: string
    max_nfts: number,
    bets_count: number,
    nfts_count: number,
    total_floor_value: number,
    escrow: string
}